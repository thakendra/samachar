[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn --chdir backend --worker-class gthread --threads 4 --workers 1 --bind 0.0.0.0:$PORT --timeout 120 server:app"
healthcheckPath = "/api/scrape/status"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
